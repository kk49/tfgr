<?xml version="1.0" encoding="iso-8859-1"?>

<root>
	<windowclass name="calendar" merge="delete"/>

	<windowclass name="calendar_empty" merge="delete"/>

	<windowclass name="calendar_select" merge="delete"/>

	<windowclass name="calendar_base" merge="delete"/>

	<windowclass name="calendar_main" merge="delete"/>
	
	<windowclass name="calendar_month" merge="delete"/>

	<windowclass name="calendar_day" merge="delete"/>
</root>
